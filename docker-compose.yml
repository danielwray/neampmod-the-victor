services:
  build:
    build: .
    environment:
      - RUSTFLAGS=-C target-cpu=x86-64-v3
    volumes:
      - .:/workspace
      - ./plugins:/output
    working_dir: /workspace
    command: |
      bash -c "
        cd xtask && cargo run -- bundle the-victor --release && cd ..
        mkdir -p /output
        cp -r target/bundled/the-victor.vst3 /output/
        cp target/bundled/the-victor.vst3/Contents/x86_64-linux/the-victor.so /output/the-victor-single.vst3
        cp target/bundled/the-victor.clap /output/
        echo 'Build completed! VST3 (bundle + single file) and CLAP plugins available in plugins/ directory'
        ls -la /output/
      "
